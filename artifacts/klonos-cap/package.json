{
  "name": "@workspace/klonos-cap",
  "version": "0.0.0",
  "private": true,
  "description": "KlonOS ZeroLag — Capacitor APK shell wrapping ClonEngine",
  "scripts": {
    "sync": "cap sync android",
    "build:web": "pnpm --filter @workspace/clonengine run build",
    "build:android": "pnpm run build:web && cap sync android && cd android && ./gradlew assembleRelease",
    "open": "cap open android",
    "cap": "cap"
  },
  "dependencies": {
    "@capacitor/android": "^7.0.0",
    "@capacitor/core": "^7.0.0",
    "@klonos/callosum-cap": "workspace:*"
  },
  "devDependencies": {
    "@capacitor/cli": "^7.0.0"
  }
}
